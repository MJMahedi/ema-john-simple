import React from 'react';

const Order = (props) => {
    console.log(props);
    return (
        <div>
            {/* <h2> summery {props.handleAddToCart}  </h2> */}
            </div>
    );
};

export default Order;